# Leave empty in local Vite — the proxy targets FastAPI on 8001.
# Set only if the API has API_KEY set (Compose nginx injects it for you).
# On Vercel, set VITE_API_URL to the public Fly/Railway API origin (no trailing slash).
VITE_API_URL=
VITE_API_KEY=
